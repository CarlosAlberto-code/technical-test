// import React from 'react'

// export const CustomInput = ({ handleChange, validation, valueName, type }) => {

//     const [isValid, setIsValid] = useState(false);
//     const [errorMessage, seterrorMessage] = useState('');

//     const setError = (msg) => {
//         seterrorMessage(msg);
//     }

//     return (
//         <input type={type} name={valueName} onChange={handleChange} onBlur={validation} value={valueName} />
//   )
// }
